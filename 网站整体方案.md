# 清华游协官网 · 整体方案

> 版本：2026-09-15 定稿
> 结论先行：**静态站做门面（GitHub Pages）+ 老站只当验证服务（Netlify）+ 企业微信群做社区 + 问卷星做报名 + 公众号发动态**。
> 本文档是唯一权威方案，做完任何一步就在对应位置打勾。

---

## 〇、总架构图

```
                     ┌──────────────────────────────┐
                     │   静态站（门面，唯一入口）      │
                     │   llw24.github.io/thu-swim-static
                     │   介绍 / 活动报名 / 加入社群 / 动态│
                     └───┬──────────┬──────────┬────┘
                         │          │          │
          看动态│        进群│     报名│          │内容怎么来的
               ▼          ▼          ▼          ▼
        ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌────────────────┐
        │ 微信公众号 │ │ 老站验证服务│ │  问卷星   │ │ content/*.md   │
        │ 「THU泳协」│ │ netlify.app│ │ 报名表单  │ │ GitHub 仓库    │
        └──────────┘ └─────┬────┘ └──────────┘ │（Issue 表单或   │
                           │ 验证通过才返回      │  /admin 可视化）│
                           ▼                   └────────────────┘
                   ┌──────────────┐
                   │ 企业微信客户群 │ ← 永久二维码 + 满200自动建群
                   │ 入群自动欢迎语 │
                   └──────────────┘
```

一句话分工：

| 部分 | 承担 | 地址 | 平时谁维护 |
|---|---|---|---|
| 静态站 | 所有对外展示 + 验证入口 + 报名入口 | `llw24.github.io/thu-swim-static` | 管理员（改 content/） |
| 老站 | 只做一件事：验证清华邮箱，通过才给群码和报名链接 | `thu-swim.netlify.app` | 技术负责人（几乎不用管） |
| 企业微信群 | 社区（聊天/约游/通知） | 微信内 | 管理员 |
| 问卷星 | 活动报名的收集 | 问卷星后台 | 管理员（每学期建一次） |
| 公众号 | 新闻动态的发布 | mp.weixin.qq.com | 管理员 |

---

## 一、四个用户流程（每个都是完整链路）

### 1. 随便看看（无门槛）
首页 → 活动报名/关于页 → 动态页顶部有公众号入口，点过去。

### 2. 进群（核心流程，要验证）
```
社区页「加入微信群」
  → 输入清华邮箱 → 老站发验证码（限 @mails.tsinghua.edu.cn / @tsinghua.edu.cn）
  → 输入 6 位验证码 → 老站校验通过
  → 页面显示：企微群永久二维码 + 群说明 + 管理员微信（备用）
  → 微信扫码进群 → 自动收到入群欢迎语（群规/报名链接/须知）
```
- 验证一次发一个 30 天有效的凭证，存在浏览器里，30 天内再来不用重新验证。
- 群二维码**不放在静态仓库**（公开仓库=公开图片），存在老站数据库里，只有验证通过的人才能从接口拿到。

### 3. 报名（要验证，复用同一次验证）
```
「活动报名」页「立即报名」→ 跳到验证页（同流程）
  → 验证通过后页面显示问卷星报名链接（链接本身不公开，只有验证过才给）
  → 填问卷星表单（姓名/学号/手机号）→ 名满即止（问卷星里设限量）
  → 管理员在问卷星后台导出名单
```
- 报名链接只通过验证接口发放，不印在任何公开页面上；就算被转出去，问卷星设了「每人限填一次」+ 名单人工核对兜底。

### 4. 管理员日常
| 事项 | 在哪做 | 频率 |
|---|---|---|
| 发新闻/改活动 | GitHub Issue 表单 或 `/admin` 可视化页 | 常用 |
| 发动态 | 公众号后台（官网动态页只放文章链接） | 常用 |
| 开报名 | 问卷星建表 → 拿链接 | 每学期 |
| 换群码/改问卷星链接 | 跑一次写入脚本（见 §五） | 少 |
| 处理进群 | 企微后台（可开入群确认/防骚扰） | 按需 |

---

## 二、老站改造清单（netlify.app，瘦身为纯验证服务）

### 保留
| 内容 | 说明 |
|---|---|
| `POST /api/auth/request-code` | 发验证码。**已有**：清华域名白名单、60秒1次、每小时5次频控。原样保留 |
| `src/lib/mail.ts` | Resend HTTP API 优先，SMTP 兜底。原样保留 |
| `src/lib/db.ts`、`ratelimit.ts` | 数据层与频控。原样保留 |
| `site_settings` 表 | 用来存群码/报名链接/微信号（key-value，表已存在） |
| `/api/health` | 健康检查 |

### 新增
| 内容 | 说明 |
|---|---|
| `POST /api/join/verify` | 入参 `{email, code}`；验码（不建账号、不设 cookie）→ 从 site_settings 读群码/报名链接 → 返回 `{ok, proof, groupQr, signupUrl, wechatId, note}`；同时记一条 `join_log`（邮箱/IP/时间，审计用） |
| `POST /api/join/gate` | 入参 `{proof}`；校验 HMAC 签名和 30 天有效期 → 返回同上内容（回访免重复验证） |
| `src/lib/cors.ts` | CORS 白名单：只允许 `https://llw24.github.io` 和 `http://localhost:3000`；处理 OPTIONS 预检 |
| `join_log` 表 | 验证审计（加进 db.ts 的 ensureInit） |
| `scripts/set-join-settings.mjs` | 一键写入/更新 site_settings 的群码、报名链接、微信号（本地跑，用 DATABASE_URL） |
| 根路径 `/` | 301 跳转到静态站，防止老链接迷路 |

### 删除（全部在 git 历史里可找回）
| 类别 | 具体内容 |
|---|---|
| 页面 | 社区 4 页、找教练、通知中心、账号页、登录页、admin、首页/新闻/训练/关于 |
| API | community/*(5)、coaches/*(3)、training/*(2)、notifications、account、reports、upload、home、news、admin、auth/verify、auth/me、auth/logout、coze/token |
| 说明 | 找教练功能删除；站内社区删除（企微群替代）；老站报名系统删除（问卷星替代）；老站新闻删除（公众号替代） |
| 数据库表 | **只删代码不删表**。posts/comments/reports 等表的数据先留着，等新方案稳定运行一学期后再清理 |

---

## 三、静态站改造清单（github.io，门面）

| 动作 | 内容 |
|---|---|
| 改 `/join` | 验证后端从 Supabase 换成老站 API（`NEXT_PUBLIC_JOIN_API`）。UI（邮箱→验证码→显示结果）复用已写好的组件。验证结果区显示：群二维码 + 报名按钮 + 微信号；二维码/链接为空时显示"入群方式将于近期开放，可先联系理事会" |
| 改 动态页 | 顶部加公众号卡片：公众号名称「THU泳协」+ 公众号二维码（放 `content/site.json`，这个可以公开）+ 说明"最新动态都在公众号"。`content/news/*.md` 继续用，但内容定位变成**公众号文章链接**（链接指向 mp.weixin.qq.com，微信内外都能打开） |
| 改 活动报名页（/training） | 「立即报名」按钮 → 跳 `/join`（验证后拿问卷星链接）。去掉"剩余名额"实时显示（问卷星没这数据），改为静态文案"名额有限，先到先得，满员后进候补请联系理事会"。`content/sessions` 里 `registerUrl` 保留为可选的**公开直链**（留空 = 走验证获取） |
| 删 | `/signup` 页 + Supabase 相关代码（`supabase.ts`、`SignupForm.tsx`；`supabase/schema.sql` 移到 `docs/archive/` 留档——它是"以后想自建报名"的现成方案） |
| 配置 | GitHub secret：`NEXT_PUBLIC_JOIN_API=https://thu-swim.netlify.app`；老站 Netlify 环境变量：`JOIN_SECRET`（签名密钥）、`DATABASE_URL`（已有） |
| 降级 | 老站接口挂了时，`/join` 显示"验证服务暂时不可用，请联系理事会成员拉你进群"——网站其余部分完全不受影响 |

---

## 四、企业微信（社区）

| 步骤 | 操作 | 注意 |
|---|---|---|
| 1 | work.weixin.qq.com 注册组织（无营业执照可注册未验证企业；建议用会长实名） | 注册后**第一件事**：确认「客户联系」功能可用 |
| 2 | 管理后台 → 客户联系 → 加客户 → **加入群聊** → 新建（二维码方式） | 生成的码**永不过期**；勾选"群人数达上限自动创建新群"；一个码最多关联 5 个群 × 200 人 = 1000 人，会员更多就再生成一个码 |
| 3 | 客户联系 → 欢迎语 → 配置入群欢迎语 | 内容建议：群规 + 本学期报名链接 + 常见问题。这是企微原生的"自动回复" |
| 4 | 下载二维码图 → 交给技术负责人写入老站 | 二维码图**不要**发到 GitHub 仓库 |
| 5 | （可选）开启入群确认 / 防骚扰 | 未验证企业的可用性需实测 |

## 五、问卷星（报名）

| 步骤 | 操作 |
|---|---|
| 1 | 创建"报名表单"：姓名、学号、院系、手机号、微信号、**清华邮箱（必填）**、备注 |
| 2 | 设置：防重复填写（每人限填一次）；名额用"限制提交数量"，免费版没有就报名截止时手动"停止收集" |
| 3 | 拿到表单链接 → 交技术负责人写入老站 `site_settings`（`join_signup_url`）。**链接不公开在任何页面** |
| 4 | 报名结束后在问卷星后台导出 Excel；**名单含手机号，不要发群里，只发脱敏版** |

### 报名门禁的强度与升级路线（重要）

门禁装在**链接发放**层（免费版能做到的）：链接只存老站数据库，验证通过才发给本人。弱点是链接可被转发，兜底是三道核对：

1. 问卷星"每人限填一次"
2. 表单必填清华邮箱 + 学号
3. **与验证名单交叉核对**：老站 `join_log` 记录了每个验证过的邮箱，问卷星导出 CSV 后脚本比对，填了未验证邮箱的直接作废（核对脚本随 Phase 1 一起交付）

两条升级路线（现阶段不做，跑一学期再决定）：

| 路线 | 效果 | 代价 |
|---|---|---|
| **A. 问卷星企业标准版** | 官方"自定义链接参数 API"：`sojumpparm=<验证邮箱>` 专属链接 + 参数签名防篡改 + 每参数限答一次 + 「只允许从自定义链接访问」（裸链接公开也无妨）。升级后是真正的**提交时验证** | 问卷星年费（需询价） |
| **B. 老站报名 API**（现成代码，见 git 历史 training/*） | 提交时校验验证凭证，门禁最硬，零年费；名单用脚本导出 | 自建系统的稳定性不如问卷星，且要重建名单导出流程 |

## 六、公众号（动态）

- 官网动态页 = 公众号入口（名称 + 二维码）+ 文章链接列表
- `content/news/*.md` 继续按老流程发（Issue 表单/管理页），但每条就是一个公众号文章链接，格式：
  ```markdown
  ---
  title: 2026 秋季零基础班开班通知
  date: 2026-09-20
  url: https://mp.weixin.qq.com/s/XXXX
  summary: 本学期第一期报名已开始
  emoji: 📰
  ---
  ```
- 公众号文章链接微信内外都能打开，这是"跳转公众号"能落地的关键（公众号主页本身没有站外可用的网址，所以入口用二维码）

---

## 七、配置全表（什么东西在哪改）

| 配置项 | 存放位置 | 改动方式 | 频率 |
|---|---|---|---|
| 站点内容（新闻/活动/公告） | `content/*.md` | Issue 表单 / `/admin` | 常 |
| 公众号名称+二维码 | `content/site.json` + `public/uploads/` | `/admin` 站点设置 | 少 |
| **群二维码 / 问卷星链接 / 微信号** | 老站 `site_settings` 表 | `scripts/set-join-settings.mjs` | 少 |
| 静态站 → 老站的接口地址 | GitHub secret `NEXT_PUBLIC_JOIN_API` | GitHub 设置 | 一次性 |
| 验证签名密钥 | Netlify 环境变量 `JOIN_SECRET` | Netlify 后台 | 一次性 |
| 发信（Resend/SMTP） | Netlify 环境变量（已配好） | — | 已完成 |
| 企微群/欢迎语 | 企微管理后台 | — | 一次性 |
| 问卷表 | 问卷星后台 | — | 每学期 |

---

## 八、实施顺序（按此执行，每步有验收标准）

### Phase 1 · 老站改造（技术负责人，约半天）
- [x] 新增 `/api/join/verify` + `/api/join/gate` + CORS + `join_log` 表（2026-09-15 完成，本地端到端验证通过）
- [x] 删除清单里的页面与 API（47 个文件）；根路径 307 到静态站
- [x] 写 `scripts/set-join-settings.mjs`（已实测写入/生效）
- **验收**：本地跑起来，curl 能看到 CORS 头；假验证码返回 403；`/api/health` 正常

### Phase 2 · 静态站改造（技术负责人，约 2–3 小时）
- [x] `/join` 换老站后端；删 `/signup` 与 Supabase 代码（schema.sql 归档到 docs/archive/）
- [x] 动态页公众号化；课程页按钮改造（→ /join?next=signup）；news 支持 url 外链
- **验收**：`npm run build` 通过；本地 dev 用老站生产接口跑通"发码→收码→显示占位内容"全流程

### Phase 3 · 配置接入（管理员，链接/二维码到位后）
- [ ] 企微注册 + 建群 + 永久码 → 脚本写入
- [ ] 问卷星建表 → 链接写入
- [ ] 公众号二维码 → `content/site.json`
- **验收**：静态站 `/join` 显示真码；扫码能进群且收到欢迎语；问卷星能填

### Phase 4 · 上线（双方）
- [ ] 老站代码 push → Netlify 自动部署；静态站 push → Pages 部署
- [ ] 手机流量（非校园网）完整走一遍：浏览 → 验证 → 进群 → 报名
- [ ] 把《管理员使用手册》更新成新流程

---

## 九、成本

| 项目 | 费用 |
|---|---|
| GitHub Pages / Netlify / 企微 / 问卷星 / Resend | 0（全部免费额度内；Netlify 函数调用量对验证场景绰绰有余） |
| 域名 | 0（全部用官方子域名） |
| 可能的将来支出 | 企微认证（约 300 元/年，可提升客户群上限）；自定义域名（几十元/年，仅当 netlify.app 将来被墙） |

## 十、风险与兜底

| 风险 | 影响 | 兜底 |
|---|---|---|
| Netlify 免费额度/账号变动 | 验证服务不可用 | 静态站自动降级为"联系理事会"；已验证用户 30 天内不受影响；可随时把老站搬回 Vercel/其他平台（代码在 git 里） |
| Resend 免费档 100 封/天 | 招新高峰验证码发不出 | 切换到已配好的 Brevo SMTP（改一个环境变量） |
| 企微未验证企业功能受限 | 客户联系/加入群聊不可用 | 退回个人微信群（接受 7 天换码），或花钱做企微认证 |
| 问卷星免费版无限额 | 名额靠人工控制 | 报名截止时手动"停止收集"；名单人工核对 |
| 验证接口被刷 | 邮件额度被耗光 | 发码接口已有频控（60s/1次、5次/时），邮箱域名白名单写死 |
| 二维码泄露 | 非清华人员进群 | 群码只通过验证接口发放；企微可开入群确认/防骚扰；管理员拉人时人工核对 |

## 十一、给管理员的维护习惯（写进手册的）

1. 每学期开学前：问卷星建表 → 把链接交给技术负责人写入；确认群码没换
2. 报名期每天看一次问卷星数据；满员就"停止收集"
3. 动态只发公众号；官网动态页记得同步贴文章链接（Issue 表单一分钟）
4. 毕业季：企微后台移除离会管理员；Netlify/GitHub/问卷星/公众号的账号交接要留记录
