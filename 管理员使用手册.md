# 管理员使用手册

> 给协会管理员看的。**你不需要会任何编程**，发内容 = 填一张网页表单。
> 所有操作都在 GitHub 网页上完成，改完网站几分钟内自动更新。

---

## 〇、开始之前（只需做一次）

1. 注册一个 GitHub 账号：<https://github.com/signup>
2. 把你的 GitHub 用户名发给技术负责人，让他把你加进 `thu-swim-static` 仓库（权限 Writer 即可）
3. 登录后打开仓库地址：<https://github.com/llw24/thu-swim-static>

## 一、发新闻 / 活动 / 社区墙内容（最常用 ✨）

1. 打开仓库 → 点顶部 **Issues** 标签
2. 点绿色按钮 **New issue**
3. 选择 **📝 发布内容** 模板
4. 按表单提示填写：
   - **内容类型**先选好（新闻 / 活动 / 社区墙），
     不相关的字段留空即可（选「新闻」就不用管"上课时间"这些）
   - 标题、正文必填；日期留空自动用今天
   - 正文支持 Markdown（想加粗就写 `**文字**`，链接写 `[名字](网址)`）
5. 提交后机器人会：
   - 自动生成网站文件
   - 关闭这个 Issue 并留言"已发布"
6. 等 **2~5 分钟** 网站自动更新完毕

> 「社区墙」类型的内容（精选文章/视频）会显示在**动态页**里，和动态合并按时间排序。

> 想撤回或修改？见下面"四、修改 / 直接编辑文件"。
>
> 💡 日常动态都在公众号「THU泳协」发布；官网动态页现在放的是**公众号文章链接**，
> 发一条动态 = 把公众号文章的网址贴进 Issue 表单即可。

## 二、管理员页面（可视化编辑，最简单 ✨✨）

打开 **网站地址/admin**（如 https://llw24.github.io/thu-swim-static/admin/）：

1. 登录：输入你的清华邮箱 → 收验证码 → 输入，即完成登录（和进群验证一样，7 天内免重复登录）
2. 之后就能：
   - 点「＋ 新增新闻」→ 填标题、日期、正文 → 保存并发布
   - 新闻可勾选「置顶」（会同步到首页卡片），可填公众号文章链接直接跳转
   - 点某条记录的「编辑」改内容，或「删除」下架
   - 切到「活动」标签管理活动：改状态（即将开展 / 已结束）、时间地点费用等信息
3. 保存后网站 2~5 分钟自动更新

> 只有白名单里的清华邮箱能登录管理页；加人/移人由技术负责人维护。
> 不再需要 GitHub Token —— 浏览器里也不保存任何密钥。

> ⚠️ 这里改的是网站**展示**。**进群二维码**存在老站的验证服务里
> （不在公开仓库），见下面「七、进群」。

## 三、发全站设置（公告横幅、联系方式、表单链接）

**首选：管理页** → 打开 `/admin/` 登录后点 **⚙️ 站点设置** 标签，改完保存即可：

- 首页黄色公告横幅 📢 的文字（清空 = 隐藏横幅）
- 页脚联系邮箱 / 微信号
- 社区页中英文介绍
- 公众号名称 / 二维码（动态页导流）

**备用：直接改文件**（管理页打不开时用）`content/site.json`，字段对照：

| 字段 | 作用 |
|---|---|
| `announcement` | 首页黄色公告横幅的文字（清空字符串即隐藏横幅） |
| `contactEmail` | 页脚联系邮箱 |
| `contactWechat` | 页脚显示的微信号 |
| `wechatGroupQr` | 仅应急用：填了就等于对所有人公开二维码，**会失去邮箱验证保护** |
| `gzhName` | 微信公众号名称（动态页导流卡片显示） |
| `gzhQr` | 公众号二维码图片路径，如 `/uploads/gzh-qr.png` |

> 进群二维码**不在这个文件里** —— 它存在老站的验证服务中，
> 只有通过清华邮箱验证的人才能拿到，见「七」。

## 四、修改或直接编辑文件

- **改正错别字**：找到对应文件（新闻在 `content/news/`，活动在 `content/sessions/`…）→ 点铅笔编辑 → Commit
- **下架一条新闻**：把文件顶部的 `draft: false` 改成 `draft: true`（彻底删除则删文件）
- **活动状态切换**：改活动文件的 `status: upcoming（即将开展）/ closed（已结束）`

## 五、启用社区页留言板（Giscus，一次性配置）

需要技术负责人操作一次：

1. 打开 <https://giscus.app> → 输入仓库名 `llw24/thu-swim-static`
2. 按页面指引给仓库开启 Discussions、安装 giscus App
3. 复制页面生成的 `data-repo` / `data-repo-id` / `data-category` / `data-category-id` 四个值
4. 填进 `content/site.json` 的 `"giscus": { ... }` 里并提交

完成后社区页底部自动出现留言墙。

## 六、活动预告怎么管

网站的活动页（「活动预告」）只做**介绍**，不在站内报名：

- **发布新活动预告**：用 Issue 表单或管理页发一条「活动」，状态选「即将开展」，
  填好时间 / 地点 / 费用 / 简介
- **活动结束后**：把状态改成「已结束」，它会自动挪到「往期活动」区
- **报名/接龙**：都在协会微信群内进行，活动详情页会提示"参加方式以群内通知为准"

## 七、进群怎么管（企业微信）

微信群用**企业微信**创建：二维码**永不过期**、群满 200 人自动建新群（一个码最多带 5 个群）、
新人进群自动收到欢迎语（群规/须知）。

### 7.1 一次性搭建

1. 用会长（或指定管理员）的微信注册企业微信组织：<https://work.weixin.qq.com>
2. 管理后台 → **客户联系 → 加客户 → 加入群聊** → 新建「通过二维码加入群聊」：
   - 新建群聊，选群主、设群名（如"清华泳协 · 招新群"）
   - 勾选「群人数达上限时，自动创建新群」
   - 下载生成的二维码图片
3. **客户联系 → 欢迎语**：配置入群自动欢迎语（群规 + 常见问题 + 提醒看群公告）
4. 把二维码图片交给技术负责人写入验证服务（**不要**传到 GitHub 仓库 —— 那是公开的）

### 7.2 日常调整

- 换群码（比如想分班建群）：企微后台重新生成 → 交技术负责人替换，立即生效
- 踢人/禁言：企微后台客户群管理里操作
- 看谁验证过：找技术负责人查老站的验证记录（邮箱 + 时间）

## 八、常见问题

| 问题 | 解决 |
|---|---|
| Issue 提交了没反应 | 检查 Issue 是否带有绿色「发布」标签；看仓库 Actions 页有没有红色 ❌ |
| 发布的内容错了 | 直接编辑生成的文件（Issue 留言里有文件路径），或者删掉重发一次 |
| 活动想下架 | 管理页把状态改成「已结束」，或直接删掉 `content/sessions/` 里对应文件 |
| 同学说收不到验证码 | 先看**垃圾邮件**；10 分钟有效，过期重新发；仍不行找技术负责人看老站日志 |
| 同学说网页打不开/很慢 | 静态站在 GitHub Pages，个别网络会慢；验证服务在 Netlify，国内一般可直连 |
| 忘记语法 | Markdown 十分钟入门：<https://markdown.com.cn> |

有其他问题在仓库提一个普通 Issue @ 技术负责人即可。
